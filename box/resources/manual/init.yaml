use: init project --name <value> [ --type [basic|cloudformation|terraform] ]
example: >
  If you want to generate a basic project:
    clencli init project --name foo
  
  If you want to generate a CloudFormation project:
    clencli init project --type cloudformation --name foo  
short: Initialize a project
long: Initialize a project with code structure and templates